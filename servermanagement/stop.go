package servermanagement

import "github.com/SUMUKHA-PK/Raft-Distributed-Consensus/types"

// StopServers kills all the running servers based on the config file
// it currently assumes all the servers are running
func StopServers(config types.Configuration) {

}
